spring:
  datasource:
    url: jdbc:tc:mysql:8.0.36:///expense_tracker_test
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
